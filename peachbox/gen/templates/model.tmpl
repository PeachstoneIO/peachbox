import peachbox.gen
import os.path

# Model definitions will be created in PROJECT_HOME/model
PROJECT_HOME = os.path.join(os.path.dirname(os.path.realpath(__file__)), '..')

# TODO
# Set project_home in peachbox.gen.model
# peachbox.gen.Model.PROJECT_HOME = os.path.join(os.path.dirname(os.path.realpath(__file__)), '..')

# Create a node
# :param
# peachbox.gen.model.define_node(name="MyNode", 
#                                identifier="itentifier", 
#                                type="type")

# Create an edge
# :param
# peachbox.gen.model.define_edge(data_unit_index=0,
#                                name="MyEdge", 
#                                node1="MyNode", 
#                                node2="MyNode2")

# Create a property
# :param
# peachbox.gen.model.define_property("MyProperty")

